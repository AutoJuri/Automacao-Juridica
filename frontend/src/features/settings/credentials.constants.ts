/** Chave da query de status compartilhada pelos cards da tela de configurações. */
export const CREDENTIALS_STATUS_QUERY_KEY = ['credentials', 'status'] as const

/** Sugestão de provedor de e-mail por domínio (Etapa 9). */
export const CREDENTIALS_PROVIDER_SUGERIDO_QUERY_KEY = ['credentials', 'provider-sugerido'] as const
